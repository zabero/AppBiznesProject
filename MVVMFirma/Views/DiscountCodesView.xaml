<local:GetAllDataView x:Class="MVVMFirma.Views.DiscountCodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CodeID" Binding="{Binding CodeID}" />
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" />
                <DataGridTextColumn Header="DiscountPercentage" Binding="{Binding DiscountPercentage}" />
                <DataGridTextColumn Header="ExpiryDate" Binding="{Binding ExpiryDate}" />
                <DataGridTextColumn Header="IsActive" Binding="{Binding IsActive}" />
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:GetAllDataView>
